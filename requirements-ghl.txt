# Opcional -- solo si vas a usar el puente con GoHighLevel (POST /ghl/webhook).
# Sin esto instalado, el endpoint responde 501 pero el resto de la API
# funciona normal (mismo patron que requirements-ocr.txt).
httpx>=0.27
